import { ActiveTaskDirective } from './active-task.directive';

describe('ActiveTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveTaskDirective();
    expect(directive).toBeTruthy();
  });
});
